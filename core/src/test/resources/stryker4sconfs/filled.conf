stryker4s {
  files: [
    "bar/src/main/**/*.scala",
    "foo/src/main/**/*.scala",
    "!excluded/file.scala"
  ]

  base-dir: "/tmp/project"

  test-runner: {
    command-runner: {
      command: "mvn clean test"
    }
  }
}