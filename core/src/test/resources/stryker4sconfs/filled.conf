stryker4s {
  files: [
    "bar/src/main/**/*.scala",
    "foo/src/main/**/*.scala",
    "!excluded/file.scala"
  ]

  base-dir: "/tmp/project"

  test-runner: {
    type=commandrunner
    command: "mvn"
    args: "clean test"
  }
}